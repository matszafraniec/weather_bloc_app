import 'package:flutter/material.dart';

class WeatherCurrentConditions extends StatelessWidget {
  const WeatherCurrentConditions({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'This is Weather current conditions list',
    );
  }
}
